import { View, Text } from 'react-native'
import React from 'react'

const  Attendance = () => {
  return (
    <View style={{flex:1, alignItems:"center", justifyContent:'center' }}>
      <Text style={{color:'red'}}>Attendance</Text>
    </View>
  )
}

export default Attendance;